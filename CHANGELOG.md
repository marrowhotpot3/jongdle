# 변경 기록

이 프로젝트를 다운로드 파일(zip)로 전달할 때마다 파일명에 버전을 붙여요
(예: `jongdle_v1.zip`). 아래는 각 버전에서 무엇이 바뀌었는지 정리한 기록이에요.

## v16 (2026-09-20)

- 도라 타일의 매수 배지 잘림 버그 수정: 도라 반짝임 효과가 적용된 타일을
  입력했을 때, 오른쪽 위에 뜨는 매수 표시가 잘려서 보이지 않던 문제를
  고쳤어요. 반짝임 효과를 그리는 방식을 바꿔서 매수 배지가 항상 온전히
  보이도록 했어요.


## v15 (2026-09-20)

- **게임 이름을 "역들"에서 "작들"로, 영문 표기를 "jongdle"로 변경**: 프로젝트
  폴더ㆍ다운로드 zip 파일명(`jongdle_vN.zip`)ㆍ페이지 타이틀ㆍ화면 상단 로고ㆍ
  도움말 문구ㆍ결과 공유 텍스트ㆍ저장(localStorage) 키ㆍLICENSE 저작권 표시까지
  전부 "작들"/"jongdle"로 통일했어요.
- **"Wordle" 관련 언급을 전부 정리**: 이전에는 README/코드 주석 여러 곳에
  "워들(Wordle)처럼"이라는 비교 설명이 남아있었는데, 상표 관련 오해 소지를
  없애기 위해 전부 지우거나 일반적인 설명으로 바꿨어요. 게임 규칙(색으로 힌트를
  주는 방식) 자체는 그대로예요.
- **소스 코드(js/css)의 개발자 주석을 전부 삭제**: 코드 동작 자체는 이전과
  똑같지만, 화면에 보이지 않는 내부 설명 주석은 모두 지웠어요.
- **저장소 폴더 이름을 `majak-wordle`에서 `jongdle`로 변경**: 깃허브에 새로
  올리실 때는 저장소 이름도 `jongdle`을 권장해요(README의 GitHub 업로드
  안내도 이에 맞춰 갱신했어요).

## v14 (2026-09-19)

- **시도 횟수를 8회에서 10회로 늘림**: 데일리ㆍ무한 모드 모두 이제 10번까지
  도전할 수 있어요. 통계의 시도 분포 막대그래프도 자동으로 10칸으로 늘어나요.
- **FAQ 칸 삭제**: 페이지 맨 아래에 자리만 있던 FAQ 칸을 완전히 없앴어요.
- **연락처 칸을 박스 없이 단순하게 변경**: 테두리ㆍ배경 박스와 "연락처"라는
  글자(제목)를 없애고, 굵지 않은(볼드 아닌) 글씨체로 "@marrowhotpot3"만
  남겼어요. 이 글자를 누르면 실제 X(트위터) 계정(https://x.com/marrowhotpot3)이
  새 탭으로 열려요.

## v13 (2026-09-19)

- **도라가 실제로 점수에 반영됨**: 지금까지는 순수 장식이었는데, 이제 손패에
  도라 패가 있으면 1장당 1판씩(같은 도라 패를 여러 장 들고 있으면 그만큼 전부)
  화료 총점 계산에 더해져요. 다만 역 목록에는 "도라"라는 항목을 따로 추가하지
  않고, 오직 총점 숫자에만 반영돼요. 역만 점수는 실제 마작 관례대로 도라의
  영향을 받지 않아요.
- **도라 하이라이트 애니메이션을 끊김 없이 이어지도록 다시 만듦**: 이전엔
  한 바퀴가 끝나고 처음으로 되돌아갈 때 위치가 어긋나 자국이 보였는데, 이제는
  시작과 끝 지점이 완전히 대칭이라 반복돼도 이어붙인 흔적이 보이지 않아요.
  속도도 일정하게(linear) 다듬었어요.
- **하이라이트 색을 흰색으로, 마작패 바탕색을 상아색으로 변경**: 도라 하이라이트가
  금빛에서 흰빛으로 바뀌었고, 패 바탕은 더 또렷한 상아색으로 다듬었어요(밝은
  테마ㆍ어두운 테마 모두).
- **제출된 손패의 패에도 도라 하이라이트 표시**: 지금까지는 역키보드에서만
  보이던 도라 하이라이트가 이제 이미 제출한(시도 횟수를 쓴) 손패의 해당 패에도
  똑같이 나타나요. 지금 입력 중인(제출 전) 손패 줄에는 여전히 나타나지 않아요.

## v12 (2026-09-19)

- **화료 점수 업다운 기호를 반대로 뒤집음**: 정답 총점보다 높을 때 ▼, 낮을 때
  ▲가 뜨도록 기호만 바꿨어요(색상은 그대로라 초록/빨강이 무엇을 뜻하는지는
  전과 동일해요).
- **역키보드(패 직접 맞추기 모드)에서 주황 상태를 없앰**: "정답에 있는 건
  확인했지만 정확한 개수는 모름"이라 주황으로 표시하던 경우도 이제 전부 초록으로
  표시해요. 역키보드는 이제 초록(포함 확인됨)ㆍ회색(부재 확정) 두 상태만 써요.
  (역 목록/팝업의 "연관된 역" 주황 표시는 이것과 별개라 그대로 남아 있어요.)
- **역만 3배 이상도 숫자 대신 이름으로 표기**: "역만"(1배)ㆍ"더블역만"(2배)까지만
  있던 걸 일반화해서, 역만급 역이 여러 개 동시에 성립해 48000의 3배ㆍ4배ㆍ6배…가
  되는 점수도 "세 배 역만"ㆍ"네 배 역만"ㆍ"여섯 배 역만"처럼 순우리말 배수
  이름으로 보여줘요.
- **도라 추가**: 새 문제(데일리ㆍ무한 모드 모두)를 시작할 때마다 34종의 패 중
  1~4개를 완전히 랜덤으로 도라로 정해요. 역키보드에서 도라로 뽑힌 패 버튼에
  금빛 하이라이트가 훑고 지나가는 애니메이션이 반복 재생돼요. 정답 손패와는
  전혀 무관한 순수 장식 요소라 점수ㆍ판정에는 영향이 없고, 제출 전에도 안전하게
  보여줘요(데일리 모드는 하루 동안 같은 도라가 유지됨). 도움말 1번 항목에
  "반짝이는 패는 도라패에요."를 추가했어요.

## v11 (2026-09-19)

- **제출 전 자가 확인 팝업의 회색(무관한 역) 표시 누락 버그 수정**: 포함된 역(초록)ㆍ
  연관된 역(주황)은 잘 표시됐는데, 과거 제출로 이미 "정답과 무관한 역"이라고
  밝혀진 역이 지금 손패에도 나타나는 경우엔 색이 안 붙고 아직 한 번도 안
  건드려본 역과 똑같이 보였어요. 이제 역 목록ㆍ제출 기록과 똑같이 회색으로
  표시돼요.
- **역 이름/설명 표기 수정**: "순쯔" → "슌쯔", "삼안커" → "산안커", 치또이쯔
  설명의 "대작(對子)" → "또이쯔".
- **데일리 통계의 시도 분포 막대그래프 버그 수정**: 막대(`.dist-bar`)가
  `<span>` 기본 속성(inline) 때문에 너비ㆍ높이 스타일이 전혀 적용되지 않아서,
  실제 값과 무관하게 막대가 안 보이고 배경만 항상 꽉 찬 것처럼 보였어요. 이제
  값에 비례해서 실제로 막대가 그려져요.
- **도움말(게임 방법) 문구를 다시 다듬었어요**: 도입부를 "역들은 무작위 손패
  14장을 맞추는 게임입니다."로 바꾸고, 역 목록 항목에서 "모두"ㆍ"처음엔 전부
  회색이에요." 문장을 지우고, 제출 전 팝업/화료 불가 설명 항목 두 개를 통째로
  삭제했으며, 무관한 역 설명 뒤 구분 문장과 역 목록 갱신 방식을 요약하던 문장도
  지웠어요.

## v10 (2026-09-19)

- **"결과 공유하기"를 다시 이모지 그리드 텍스트 복사 방식으로 되돌렸어요.** v9에서
  잠깐 이미지 복사로 바꿨던 걸 되돌려서, 예전처럼 🟩🟨⬛ 이모지로 표시된 시도별
  기록을 클립보드에 텍스트로 복사해요(역 이름은 여전히 노출되지 않음).
- **"X에 공유하기" 버튼을 새로 추가했어요.** 결과 팝업에 "결과 공유하기" 바로
  옆에 생겼고, 누르면 같은 이모지 그리드 텍스트를 미리 채운 X(트위터) 글쓰기
  화면이 새 탭으로 열려요. 여기에도 역 이름ㆍ한자 같은 정답 정보는 들어가지
  않아요.
- (버그 수정) 이모지 그리드를 시도당 7개씩 두 줄로 나누던 로직이 문자열을
  글자 수가 아니라 코드유닛 단위로 잘라서, 드물게 🟩/🟨 이모지가 그 경계에서
  반으로 깨질 수 있었어요(클립보드 텍스트 복사에서는 티가 안 났지만, 이번에
  추가한 X 공유용 URL 인코딩에서는 오류가 났어요). 이모지를 자르기 전에
  배열 상태에서 먼저 7개 단위로 나누도록 고쳤어요.

## v9 (2026-09-19)

- **역키보드(패 버튼)의 잘못된 주황색 표시 버그 수정**: 정답 손패를 그대로 제출해서
  단번에 맞힌 경우(예: 역패 · 중 3장을 포함한 정답을 그대로 내서 맞힘)처럼, 어떤
  시도 자체가 정답 손패와 완전히 같아지면 그 순간 정답 손패 14장 전체를 이미 정확히
  알고 있는 것과 같아요. 예전엔 이런 경우에도 "초과로 더 내본 적 없음" 조건에 걸려
  역키보드에 주황(부분 확인)만 계속 남아있었는데, 이제는 제출 기록에서처럼 정답에
  포함된 패 종류는 즉시 초록(확정), 나머지는 즉시 회색(부재 확정)으로 표시돼요.
- **게임이 끝나면 성공/실패를 한눈에 알 수 있게 개선**: 결과 팝업 맨 위에
  "🎉 성공!" 또는 "😵 실패 (8회 모두 소진)"이 색과 함께 크게 표시되고, 팝업을
  닫아도 "결과 보기" 배지 자체가 초록(성공)ㆍ빨강(실패)으로 물들며 문구도 함께
  바뀌어요. 굳이 팝업을 다시 열어보지 않아도 화면 위쪽만 보고 바로 알 수 있어요.
- **"결과 공유하기"의 방식을 텍스트(이모지 그리드)에서 이미지로 변경**: 이제 버튼을
  누르면 마지막 정답 손패(14장)를 패 그림 그대로 담은 이미지가 클립보드에
  복사돼요. 지금 테마(라이트/다크) 색을 그대로 따라가고, 제목 줄에는 날짜ㆍ모드ㆍ
  성공/실패ㆍ시도 횟수만 들어가고 역 이름ㆍ한자는 절대 들어가지 않아요. 이미지
  클립보드 복사를 지원하지 않는 구형 브라우저에서는 대신 새 탭에 이미지를 열어줘요.

## v8 (2026-09-19)

- **마작패 문양을 다시 단순한 모습으로 되돌렸어요.** v7에서 실물 참고 이미지에
  맞춰 그렸던 바퀴/만다라 무늬의 통(筒)패, 마디 있는 방추 모양의 삭(索)패, 새
  그림이 있던 1삭 등 장식적인 문양을 전부 빼고, 통패는 단순한 채워진 원, 삭패는
  단순한 막대로 다시 그렸어요. 참고 이미지에서 가져온 정보(점/막대의 위치ㆍ
  개수ㆍ남색-빨강 구성)는 그대로 유지했어요 — "배치"만 참고하고 "문양"은
  단순하게 그린다는 원칙이에요.
- **손패 14장을 다 채우면 제출 전에도 화료 시 점수를 미리 보여줘요.** 제출 전
  자가 확인 팝업(이 손패로 성립하는 역 목록) 아래에 "화료 시 점수: OOO"가
  추가로 뜨는데, 지금 이 손패만 보고 계산한 점수라 정답 손패와는 비교하지
  않아요(▲/▼/＝ 없음) — 제출 후 기록에만 붙는 비교 표시와는 다름.
- **화료 시 점수 계산 방식을 다시 확인했어요.** 성립하는 역들을 하나하나 따로
  더하는 게 아니라, 그 손패로 실제 마작을 플레이했을 때 오야가 받는 점수(판수를
  전부 합쳐 점수표에서 한 번에 찾는 방식)를 그대로 쓰고 있는지 점검했는데,
  이미 v5부터 그렇게 계산하고 있어서 별도로 고칠 부분은 없었어요.
- **제출 기록의 "미포함 역" 칩에 회색이 안 입혀지던 버그 수정**: 포함된 역(초록)ㆍ
  연관된 역(주황)에는 색이 잘 들어갔는데, 정답과 무관한 역에는 색상 클래스가
  아예 빠져 있어서 역 목록의 "무관한 역" 배지와 다르게 밋밋하게 보이던 문제를
  고쳤어요. 이제 세 가지 상태 모두 역 목록과 똑같은 색으로 보여요.
- **"패 직접 맞추기 모드" 추가**: 역을 모두 맞히고 타일 힌트가 해금된 뒤에는,
  제출한 기록의 패뿐 아니라 화면 아래 역키보드(패 버튼)의 패에도 지금까지
  알아낸 정보에 따라 색이 들어가요. 초록은 정답에 있는 정확한 개수까지 확인된
  패, 주황은 정답에 있다는 것만 확인되고 정확한 개수는 아직 모르는 패, 회색은
  정답에 전혀 없다고 확인된 패예요.
- **도움말 내용을 요청받은 대로 다듬었어요** — 더 이상 필요 없어진 세부 설명과
  중복 문구를 여러 군데 정리하고, 화료 점수 관련 항목은 "정답 손패의 점수보다
  높은지(▲)ㆍ낮은지(▼)ㆍ같은지(＝)도 함께 알려준다"는 내용으로 다시 썼어요.
  (다음에 다시 요청하기 전까지 도움말 문구는 더 손대지 않아요.)
- **역 목록 위 "역 목록" 글자를 지웠어요.** 범례(포함된 역/연관된 역/무관한 역)만
  남고 제목 텍스트는 삭제.
- **게임 안의 모든 패 크기를 50% 키웠어요.** 입력칸ㆍ역키보드ㆍ제출 기록ㆍ정답
  공개 팝업의 패가 모두 더 커졌어요. 이제 14장이 화면 폭에 따라 자연스럽게
  여러 줄로 줄바꿈될 수 있어요(가로 스크롤은 생기지 않음) — 한 줄 고정이던
  이전 제약은 이번 확대에 맞춰 없앴어요.
- **제출 버튼 문구를 "이 손패 제출"에서 "제출"로 줄였어요.**
- **"한 칸 지우기" 버튼을 없앴어요.** 패를 하나만 지우고 싶으면 입력칸에 놓인
  그 패를 직접 눌러서 뺄 수 있어요(기존에도 가능했던 기능).
- **현재 시도 중이던 데일리 게임 기록을 초기화했어요.** 데일리 저장 키 버전을
  올려서, 이번 업데이트 이후에는 이전에 진행 중이던 데일리 기록이 자동으로
  무시되고 새로 시작돼요(지난 통계는 그대로 유지).
- **큰 점수를 숫자 대신 실제 마작 용어로 표기해요.** 12,000점은 "만관",
  18,000점은 "하네만", 24,000점은 "배만", 36,000점은 "삼배만", 48,000점은
  "역만", 96,000점(더블역만급)은 "더블역만"으로 표시돼요. 이름이 따로 없는
  1~4판(1,500~11,600점)은 지금처럼 숫자로 표시돼요.

## v7 (2026-09-16)

- **점수 표기를 "화료 시 총점 하나"로 단순화**했어요. v5부터 있던 "개별 역마다
  점수 표시" 방식을 없애고, 실제 마작 채점 방식대로 한 시도가 동시에 성립시키는
  역을 전부 합친 총점 하나만 제출 기록마다 보여줘요(역만이 섞이면 역만끼리
  합산, 아니면 판수를 전부 더해 점수표에서 찾아요). 정답 손패의 총점과 비교한
  ▲/▼/＝ 표시는 그대로예요.
- **도움말을 처음부터 다시 썼어요.** 예전 12회 시도ㆍ개별 역 점수 등 낡은 내용을
  전부 지우고, 지금 게임 방식(패 고르기 → 자가 확인 팝업 → 제출 → 역/점수/타일
  힌트 공개 → 8회 안에 정답 맞히기)을 처음부터 순서대로 다시 설명해요.
- **마작패 문양을 실물 참고 이미지에 맞춰 다시 그렸어요.** 만수패는 숫자 잉크색이
  검정, "萬" 글자가 빨강으로 바뀌었고(예전엔 반대였어요) 5는 갖은자 "伍"를 써요.
  통(筒)패는 단순한 원이 아니라 바퀴/만다라 모양의 점으로, 삭(索)패는 마디가 있는
  방추 모양 막대로 다시 그렸고, 1통ㆍ1삭ㆍ8삭 같은 특수 문양과 각 숫자별
  남색/빨강 배치도 참고 이미지에 맞게 손봤어요.
- **"역키보드" 글씨를 지웠어요.** 패를 고르는 영역 위에 있던 제목 텍스트만
  삭제했고, 기능은 그대로예요.
- **시도 횟수를 12회에서 다시 8회로 되돌렸어요.**
- **타일(패) 힌트가 새로 추가됐어요.** 어떤 제출에서 잘못 포함된 역 없이 정답
  손패가 성립시키는 역을 전부 맞히면, 그 시도부터는 제출 기록의 패에도 초록(정답
  손패에 포함된 패)ㆍ회색(포함되지 않은 패) 색이 나타나요. 이미 그 제출로
  합법적으로 얻은 정보라 안전하고, 지금 입력 중인(제출 전) 손패에는 절대
  나타나지 않아요.
- **메인 제목 아래 작게 붙어있던 "役ㆍ들" 부제를 지웠어요.**

## v6 (2026-09-16)

- **패 크기를 다시 키웠어요.** 입력칸ㆍ제출 기록의 패와 정답 공개 팝업의 패 모두,
  너무 작다는 의견을 반영해서 예전 크기와 지금 크기의 중간 정도로 키웠어요(14장이
  한 줄에서 줄바꿈되지 않는 선까지는 유지).
- **"무한 연습" 탭 이름을 "무한 모드"로 바꿨어요.**
- **백(白) 패의 문양을 삭제**했어요. 예전엔 장식 테두리가 그려져 있었는데, 그건
  중국식 패에 가까운 표현이라 리치 마작 패 관례대로 완전히 빈 패로 바꿨어요.
  (동남서북발중 자패는 그대로예요.)
- **8통ㆍ8삭 패의 점/막대 배치를 3×3에서 2행×4열로 수정**했어요. 실제 통ㆍ삭 패
  인쇄 관례에 맞는 배치예요.
- **만수패의 숫자를 한자 숫자(一二三四五六七八九)로 바꿨어요.** 그 아래 "萬" 글자는
  그대로예요.
- **역 목록 옆 범례 문구를 정리**했어요. "초록 정답의 역 · 주황 정답과 연관된 역 ·
  회색배경 제출했지만 무관한 역" 같은 설명 문장 대신, 각 색이 칠해진 칩에
  "포함된 역"/"연관된 역"/"무관한 역"이라고만 짧게 표시해요.

## v5 (2026-09-16)

- **역 설명(hover 툴팁)에서 한자 표기를 삭제**했어요. 이제 판수와 간단한 설명만 떠요.
- **연관 역(주황) 판정을 대칭적으로 확장**했어요. 예전에는 정답 손패 자체가 구조적으로
  "더 엄격한 역"까지 우연히 함께 만족할 때만(예: 정답=준찬타 → 찬타도 연관 역) 주황이
  붙었는데, 이제는 반대 방향도 성립해요 — 정답이 찬타"만" 성립하는 손패여도 준찬타는
  연관 역으로 잡혀요. 역패(백ㆍ발ㆍ중)도 소삼원ㆍ대삼원과 새로 연관 역으로 묶었어요
  (실제 마작 규칙에서 대삼원이 역패를 대체하는 건 아니고 같이 점수가 매겨지므로,
  역 판정 엔진 자체(`yakuEngine.js`)는 건드리지 않고 게임 UI의 "연관 역 표시" 로직에만
  이 관계를 추가했어요).
- **정답 제출 팝업(제출 전 자가 확인용)에 이미 밝혀진 역이 있으면 같은 색을 표시**해요.
  과거에 제출(시도 소모)해서 이미 초록/주황으로 확인된 역이 지금 만들고 있는 손패에도
  나타나면, 팝업에서도 중립색이 아니라 그 초록/주황을 그대로 보여줘요. 이미 합법적으로
  공개된 정보를 다시 보여주는 것뿐이라 안전해요.
- **시도 횟수를 8회에서 12회로 늘렸어요.**
- **제출 기록에 나오는 각 역 옆에 화료 시 점수(오야 기준)를 표시**하고, 그 점수가
  정답 역의 점수보다 높은지(▲)ㆍ낮은지(▼)ㆍ같은지(＝)도 함께 보여줘요. 실제 마작
  점수는 판수와 부수를 함께 봐야 정해지는데 이 게임은 손패 "모양"만 다루고 대기
  모양ㆍ멘젠/후로 같은 세부 상황을 추적하지 않아서, 표준 30부를 대표값으로 가정한
  오야 기준 점수표를 쓴 단순화된 값이에요 (역만은 48,000점, 대사희 같은 더블역만은
  96,000점으로 고정).

## v4 (2026-09-20)

- **정답 공개를 별도의 팝업 창으로 분리**: 게임이 끝나면(성공/실패 모두) 정답
  손패ㆍ결과 공유하기ㆍ다음 문제가 이제 페이지에 계속 붙어있는 영역이 아니라
  독립된 팝업 모달로 자동으로 떠요. 닫아도 시도 표시 배지 옆에 새로 생긴
  "결과 보기" 배지를 눌러 언제든 다시 열 수 있어요.
- **역 이름에 hover 설명 추가**: 역 목록ㆍ자가 확인 팝업ㆍ제출 기록에 나오는
  모든 역 이름 위에 마우스를 올리면(모바일은 길게 누르면) 한자ㆍ판수ㆍ간단한
  설명이 담긴 툴팁이 떠요.
- 페이지 맨 아래에 있던 "자세한 게임 방법은 왼쪽 위 ? 버튼에서 확인하세요."
  안내 문구(푸터)를 삭제했어요.
- **처음 접속 시 게임 방법 팝업 자동 표시**: 이 브라우저에서 처음 접속하면
  게임 방법 팝업이 자동으로 떠요. 한 번 보면 다음 방문부터는 자동으로 뜨지
  않고, "?" 버튼으로 언제든 다시 볼 수 있어요.
- 페이지 맨 아래에 **FAQ / 연락처 칸을 새로 추가**했어요. 지금은 내용 없이
  자리만 있고, 내용은 추후에 채워질 예정이에요.

## v3 (2026-09-19)

- **패 크기 재조정**: v2에서 너무 작게 줄였던 패 크기를 다시 키웠어요. "14장이
  한 줄에서 줄바꿈되지 않는 선에서 최대한 크게"를 기준으로 다시 계산해서
  입력칸/기록/결과 패널 모두 더 읽기 편해졌어요.
- 제출 전 자가 확인 팝업 위에 있던 **"이 손패로 성립하는 역 — 정답과는 무관..."
  설명 문구를 삭제**했어요 (칩 목록만 남음).
- **역 목록에 "시도했지만 무관한 역"용 회색 배경을 새로 추가**: 지금까지는
  제출해서 실제로 성립시켰지만 정답과 무관한 역도 "아직 한 번도 안 건드려본
  역"과 똑같이 테두리만 있는 연한 회색으로 보였는데, 이제는 진한 회색 배경으로
  채워져서 워들 키보드의 회색(탈락) 글자처럼 확실히 구분돼요.
- **치또이쯔ㆍ량페코 동시 판정 버그 수정**: 량페코 대표 손패(런 2쌍)가 우연히
  "서로 다른 7종 패가 2장씩"이라는 치또이쯔 개수 조건도 충족해서 치또이쯔까지
  함께 뜨던 문제를 고쳤어요. 이제 표준 4세트+머리로 분해되는 손패는 치또이쯔로
  판정하지 않아요 (하위 역 관계가 아니라 아예 별개의 해석이라 동시에 뜨면 안 됨).
- **자일색ㆍ혼일색 동시 판정 버그 수정**: 자패로만 이루어진 손패(자일색)가
  "슈트 한 종류 이하 사용"이라는 혼일색 조건도 구조적으로 만족해서 혼일색까지
  함께 뜨던 문제를 고쳤어요. 청일색이 혼일색을 대체하는 것과 같은 방식으로,
  자일색이 성립하면 혼일색은 결과에서 완전히 제외돼요.
- **결과 패널을 정답 손패 + 버튼만 남기고 정리**: "OOO 완성!"/"아쉬워요 — 정답은
  OOO" 같은 문구와 한자ㆍ판수ㆍ설명 텍스트를 지우고, 정답 손패 패 그림과
  "결과 공유하기"/"다음 문제" 버튼만 남겼어요.

## v2 (2026-09-18)

- **제출 전 자가 확인 팝업 재도입**: 손패 14장을 다 채우면 입력칸 바로 아래에
  "이 손패로 성립하는 역" 팝업이 다시 떠요. v1에서 완전히 없앴던 팝업을, 이번엔
  "정답 손패와는 절대 비교하지 않고 지금 만든 손패만 보고 판정"하는 형태로 다시
  넣었어요 — 플레이어가 실제 마작 규칙만 알면 스스로도 계산 가능한 정보라 제출
  (시도 소모) 전에 봐도 정답을 누설하지 않아요. 화료 불가 손패면 팝업에도
  "성립하는 역이 없어요"라고 표시됨.
- **제출 기록(history)에서 더는 역을 지우지 않음**: 이전에는 제출한 손패가
  실제로 성립시키는 역 중 정답과 겹치지 않는 건 통째로 숨기고 "정답과 겹치는
  역 없음"이라고만 표시했는데, 이제는 성립시킨 역을 전부 보여주고 그중 정답과
  겹치는 역만 초록/주황으로 강조, 나머지는 회색으로 남겨둬요.
- **손패 14장을 채웠는데 자가 확인 팝업이 안 뜨던 버그** 수정.
- **패 입력칸 크기 축소**: 뷰포트 폭에 따라 패 크기를 더 촘촘하게 줄여서,
  14장이 한 줄에서 밀려 줄바꿈되던 문제를 해결.
- **기록 번호 배지 위치 변경**: 시도 번호가 더는 패가 있는 네모칸 "안"에서
  자리를 차지하지 않고, 네모칸 바깥 왼쪽 위에 작은 배지로 겹쳐 표시돼요.
- **결과 패널에 정답 손패 14장을 실제 패 그림으로 공개**: 기존엔 역 이름만
  텍스트로 보여줬는데, 이제 게임이 끝나면(성공/실패 모두) 정답으로 쓰인 손패
  14장을 그대로 타일 그림으로 볼 수 있어요.
- **페이지 맨 아래 설명 문구를 도움말 모달로 통합**: 푸터에 있던 긴 설명은
  전부 도움말("?" 버튼) 모달에 이미 담겨 있던 내용이라 중복 표시하지 않도록
  푸터를 "?" 버튼을 안내하는 한 줄로 줄임.

## v1 (2026-09-17)

- **정답 겹침 기반 역 목록** 신설: 역키보드 바로 아래에 마작 역 28종을 항상 나열하고,
  처음엔 모두 회색으로 표시. 제출한 손패가 정답 손패와 실제로 겹치는 역을 발견할
  때마다 그 역만 색이 바뀜(초록 = 정답이 실제로 성립시키는 역, 주황 = 정답과
  구조적으로 연관되지만 상위 역에 밀린 역) — 워들의 온스크린 키보드처럼 발견 내용이
  누적됨.
  - 이전에는 "내가 제출한 손패가 성립시키는 역"을 그대로 초록으로 보여줬는데, 이제는
    반드시 **정답 손패와 겹치는 경우에만** 색이 바뀌도록 수정.
- 제출하기 전(패를 14장 채운 직후 포함)에는 어떤 판정 결과도 화면에 노출되지 않음 —
  시도 횟수를 쓰지 않고 정답을 유추하지 못하도록 하는 설계를 유지.
- 제출한 시도 기록(#history)에도 정답과 겹치는 역만(초록/주황) 표시하도록 통일.

## 이전 변경 사항 (버전 태그 없이 진행됨)

- 타일 아트를 실제 마작패와 비슷한 SVG 문양으로 교체 (통/삭/만/자패).
- 역키보드를 "새 문제" 배지 바로 아래로 이동, 제출한 손패를 최신순으로 기록하는
  #history 섹션 추가.
- 14장을 채운 손패가 애초에 화료 불가능한 형태면 제출 버튼을 "화료 불가"로 바꾸고
  비활성화.
- 타일(패) 자체에 위치 비교 기반 초록/노랑/회색 색상을 주던 기능을 완전히 제거 —
  실제 마작에는 "자리" 개념이 없고, 제출 전에 이 정보가 노출되면 시도 횟수 없이
  정답을 알아낼 수 있는 문제가 있었음.
- 상위 역(예: 청일색)이 성립하면 하위 역(예: 혼일색)을 결과에서 완전히 제외하도록
  판정 엔진 수정 (`resolveDisplayYaku`).
- 역키보드 카운트 배지가 0이 되어도 CSS 때문에 숫자가 계속 보이던 버그 수정
  (`[hidden]`이 작성자 `display` 규칙에 밀리던 문제).
